import { useState } from "react";

function filterCat () => {
    const filterCategory = useState(null);

    return(
        if (!filterCategory){
            <section>
                <div></div>
            </section>
        }
    )
}